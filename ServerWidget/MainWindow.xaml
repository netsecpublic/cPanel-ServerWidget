<Window x:Class="ServerWidget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Server Status Widget" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" 
        Background="Transparent" WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseRightButtonUp="Window_MouseRightButtonUp">

    <Border Background="#1A1A1A" CornerRadius="10" Padding="8" BorderBrush="#333333" BorderThickness="1">
        <ItemsControl x:Name="ServersContainer">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding BarBackground}" CornerRadius="6" Margin="0,3" Padding="10,6" Width="260"
                            MouseRightButtonUp="ServerBar_MouseRightButtonUp">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Ellipse Grid.Column="0" Width="10" Height="10" Fill="{Binding StatusColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Column="2" Text="{Binding LoadText}" Foreground="#D0D0D0" FontSize="11" FontWeight="Medium" VerticalAlignment="Center" Margin="6,0,8,0"/>
                            <TextBlock Grid.Column="3" Text="{Binding QueueText}" Foreground="#FFFFFF" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</Window>